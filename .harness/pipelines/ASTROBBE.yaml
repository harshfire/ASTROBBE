version: 1
name: Astrobee-New
stages:
  - name: build
    type: ci
    spec:
      steps:
        - name: Run
          type: script
          spec:
            run: |-
              sleep 100000
              chmod 777 ./scripts/docker/build.sh
              ./scripts/docker/build.sh --bionic --remote astrobee test_astrobee
